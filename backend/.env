# --- Server
PORT=3000
NODE_ENV=production

# --- JWT
JWT_SECRET="KUCHTOHAITUJMERABTAKUCHTOHAITUJMELAPATA,JAIHIND"
JWT_EXPIRES_IN="7d"

# --- Redis (Upstash) — used for leaderboard ZSETs
REDIS_URL="rediss://default:gQAAAAAAAaLEAAIgcDJjNDMwZjU3NjIzOGE0NzgzYjBjN2U0YjI4MmFjOWRlZQ@wondrous-elephant-107204.upstash.io:6379"

# --- Database (Postgres via docker-compose)
# host = service name "db" inside the compose network
DATABASE_URL="postgresql://locus:hEY_CHANGE_ME_STRONG_PASSWORD@db:5432/locus?schema=public"

# --- Admin API (used by the Next.js admin dashboard)
ADMIN_API_TOKEN="hEY_CHANGE_ME_ADMIN_TOKEN"

# --- Postgres credentials (used by the db service)
POSTGRES_USER=locus
POSTGRES_PASSWORD=hEY_CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=locus

# --- Host port the backend binds to (loopback only by default)
HOST_PORT=3001
